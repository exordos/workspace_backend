@startuml
!pragma layout smetana
skinparam shadowing false
title GET /api/workspace/v1/messenger/folder_items/{folder_item_uuid}
actor "The client ." as Client
participant "Controller is RestAlchemy" as API
database "PostgreSQL" as DB
Client -> API: The request
API -> DB: - It 's indexed . SELECT
note right of DB
  Canonical stream not copied
  Normalized FOLDER_ITEM = source of truth; folder_projection He 's building . snapshot
  It 's automatic . FOLDER_ITEM — The projection of the worker is restored
  The source: active USER_STREAM_BINDING + It 's canonical . STREAM.is_archived = false
  All chats = All of them .; Personal = STREAM.private = true; Channels = STREAM.private = false
  One . indexed join I 'm not ready USER_STREAM_BINDING; without N+1/COUNT/custom SQL
end note
DB --> API: public lines
note right of API: without outbox / tasks / events
API --> Client: 200 JSON
@enduml
