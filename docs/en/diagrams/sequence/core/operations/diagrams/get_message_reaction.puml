@startuml
!pragma layout smetana
title GET /api/workspace/v1/messenger/message_reactions/[reaction_uuid]
actor "The client ." as Client
participant "WorkspaceMessageReactionController\nRestAlchemy" as API
database "Indexed public presentation\n+ physical lines of external keys" as DB
Client -> API: GET /api/workspace/v1/messenger/message_reactions/{reaction_uuid}
API -> DB: reaction_uuid + Applicable placement;\njoin active stream membership + generation
DB --> API: lines without calculations
API --> Client: 200, public JSON
note over API,DB
No recording . / transactional outbox / The background artist / the events
No aggregating or restoring during query
Public message_uuid = placement UUID; How to restore placement context
For the reaction_uuid It 's still centralized . OPEN-The decision.
end note
@enduml
